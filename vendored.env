# Ensure go and operator uses vendored dependencies (the default when
# a vendor path is present).
# TODO(marun) Remove -mod flag rather than clearing
export SHIST_NO_VENDOR=
export GOFLAGS=
export GO_MOD_FLAGS=
